<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0, user-scalable=yes">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="format-detection" content="telephone=no">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <title>管理后台 - 导入圣经数据</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>⚙️ 管理后台</h1>
        
        <% if (message) { %>
            <div class="message <%= message.indexOf('错误') >= 0 || message.indexOf('Error') >= 0 ? 'error' : 'success' %>">
                <%= message %>
            </div>
        <% } %>
        
        <!-- 功能导航 -->
        <div class="admin-nav-grid">
            <a href="/admin/prompts" class="admin-nav-btn">
                <span class="admin-nav-icon">🤖</span>
                <span class="admin-nav-label">提示词管理</span>
            </a>
            <a href="/admin" class="admin-nav-btn active">
                <span class="admin-nav-icon">📤</span>
                <span class="admin-nav-label">圣经导入</span>
            </a>
        </div>
        
        <h2>导入 Excel 数据</h2>
        
        <form method="post" action="/admin/upload" enctype="multipart/form-data">
            <p>
                <label for="file">选择 Excel 文件：</label>
                <input type="file" id="file" name="file" accept=".xlsx,.xls,.csv" required />
            </p>
            <p>
                <button type="submit">📤 上传并导入</button>
            </p>
            <p style="color: #e74c3c; font-size: 0.9rem;">
                ⚠️ 注意：导入将会清空现有的所有圣经数据
            </p>
        </form>
        
        <div class="nav-links">
            <a href="/">返回首页</a>
        </div>
    </div>
    <script src="/js/main.js"></script>
</body>
</html>
